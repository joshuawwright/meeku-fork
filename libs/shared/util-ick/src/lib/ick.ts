export enum Condition {
  withIck = 'With I Cannot Know',
  withoutIck = 'Without I Cannot Know',
  combined = 'Combined (Without ICK followed by With ICK)'
}

export const CONDITIONS = Object.values(Condition);
